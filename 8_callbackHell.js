/**
 * The problem with the approach described in 5 - 6 videos is that
 * the only place where you have access to the data coming from the API request
 * is in the callback function
 * imaging habing another async function in that function, and then another one,
 * that's the callback hell
 */
